FoamFile
{
    format ascii;
    class volVectorField;
    location "0";
    object fD;
}
dimensions [1 -1 -2 0 0 0 0];
internalField uniform (0 0 0);
boundaryField
{
    fixedWalls { type calculated; value uniform (0 0 0); }
    movingWall { type calculated; value uniform (0 0 0); }
    front { type cyclic; }
    back  { type cyclic; }
}
